package main

// Placeholder: add Postgres persistence here when you’re ready.
// Suggested functions (using pgx):
//
// func InsertOnEnqueue(ctx context.Context, db *pgxpool.Pool, envlp Envelope) error
// func MarkSuccess(ctx context.Context, db *pgxpool.Pool, id string) error
// func MarkFailure(ctx context.Context, db *pgxpool.Pool, id string, attempt int, errMsg string) error
//
// Keep this file so your build succeeds without DB until we wire it.
